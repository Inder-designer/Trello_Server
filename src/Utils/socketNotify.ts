// import { io } from '../server';

// export function notifyBoardOwner(ownerId: string, payload: any) {
//     // ownerId should be used as a room or socket id in your client logic
//     io.to(ownerId).emit('board-join-request', payload);
// }
